#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ember-cli-windows/bin/ember-cli-windows" "$@"
  ret=$?
else 
  node  "$basedir/../ember-cli-windows/bin/ember-cli-windows" "$@"
  ret=$?
fi
exit $ret
